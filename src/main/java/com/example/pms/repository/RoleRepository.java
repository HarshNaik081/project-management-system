package com.example.pms.repository; import com.example.pms.model.Role; import com.example.pms.model.ERole; import org.springframework.data.jpa.repository.JpaRepository; import java.util.Optional;
public interface RoleRepository extends JpaRepository<Role, Long> { Optional<Role> findByName(ERole name); }